parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phpcs: 
            standard: PSR2
            show_warnings: true
            tab_width: 4
            ignore_patterns: []
            sniffs: []
            triggered_by: [php]
        phpcsfixer: ~
        phpunit: 
            config_file: 'phpunit.xml'
